pool:
  vmImage: 'Ubuntu 18.04'

steps:
- bash: |
    git clone --bare https://${USER}:${PASS}@dev.azure.com/henrikbinggl/MonoRepo/_git/MonoRepo ${BUILD_SOURCESDIRECTORY}/mirror
    cd ${BUILD_SOURCESDIRECTORY}/mirror
    git push --mirror https://bihe:${TOKEN}@github.com/bihe/monorepo.git 
  displayName: 'mirror source to github'