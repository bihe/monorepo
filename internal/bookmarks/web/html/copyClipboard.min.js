!function () { const t = document.querySelectorAll(".copy-clipboard-btn"); if (t) for (let o = 0; o < t.length; o++) { const e = t[o]; e.addEventListener("click", (t => { t.preventDefault(); try { var o = e.getAttribute("data-clipboard-text"); if (o) { navigator.clipboard.writeText(o); var a = { notification_message: `Copied '${o}' to clipboard!` }; htmx.ajax("POST", "/bm/toast", { swap: "none", values: a }) } else console.log("could not get a URL to copy") } catch (t) { alert("Could not copy URL to clipboard!"), console.error(t.message) } })) } }();
